name: Release @kitiumai/scripts

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:
    inputs:
      tag:
        description: 'Tag to release (e.g., v1.0.0)'
        required: true
        type: string

permissions:
  contents: write

jobs:
  release-scripts:
    name: "Release @kitiumai/scripts"
    runs-on: ubuntu-latest
    container:
      image: docker.io/ashishyd/kitiumai-dev:latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Setup Git
        run: |
          id-token: write
          git config --global user.name "github-actions[bot]"
          git config --global user.email "github-actions[bot]@users.noreply.github.com"
          git config --global --add safe.directory $GITHUB_WORKSPACE

      - name: Environment health check
        run: |
          echo "📋 Environment diagnostics:"
          echo "Node: $(node --version)"
          echo "NPM: $(npm --version)"
          echo "Git: $(git --version)"
          echo "Working directory: $(pwd)"
          echo "User: $(whoami)"

      - name: Ensure pnpm available (corepack fallback)
        run: |
          if ! command -v pnpm >/dev/null 2>&1; then
            echo "ℹ️ pnpm not found, enabling via corepack"
            corepack enable
            corepack prepare pnpm@8 -o /usr/local/bin/pnpm
          fi

      - name: Get tag name
        id: tag
        run: |
          if [ "${{ github.event_name }}" = "push" ]; then
            TAG_NAME="${GITHUB_REF#refs/tags/}"
          else
            TAG_NAME="${{ inputs.tag }}"
          fi
          echo "tag=$TAG_NAME" >> $GITHUB_OUTPUT
          echo "Processing tag: $TAG_NAME"

      - name: Extract version from tag
        id: version
        run: |
          # Extract version from tag like v1.2.3
          VERSION=$(echo "${{ steps.tag.outputs.tag }}" | sed 's/^v//')
          echo "version=$VERSION" >> $GITHUB_OUTPUT
          echo "Extracted version: $VERSION"

      - name: Install dependencies
        run: pnpm install --frozen-lockfile --prefer-offline --ignore-scripts

      - name: Build package
        run: |
          WORKDIR="tooling/scripts"
          if [ -d "$WORKDIR" ]; then cd "$WORKDIR"; else echo "ℹ️ tooling/scripts not found; using repo root"; fi
          pnpm run build

      - name: Run tests
        run: |
          WORKDIR="tooling/scripts"
          if [ -d "$WORKDIR" ]; then cd "$WORKDIR"; fi
          pnpm run test

      - name: Run linting
        run: |
          WORKDIR="tooling/scripts"
          if [ -d "$WORKDIR" ]; then cd "$WORKDIR"; fi
          pnpm run lint

      - name: Type check
        run: |
          WORKDIR="tooling/scripts"
          if [ -d "$WORKDIR" ]; then cd "$WORKDIR"; fi
          pnpm run type-check

      - name: Security checks
        run: |
          WORKDIR="tooling/scripts"
          if [ -d "$WORKDIR" ]; then cd "$WORKDIR"; fi
          # Run security scans if the module exists; otherwise skip (non-blocking)
          if [ -f "dist/security/index.js" ]; then
            node --input-type=module -e "
              import { scanSecrets } from './dist/security/index.js';
              const result = await scanSecrets({ failOnFinding: true });
              if (result.findings?.length) {
                console.error('Security issues found:', result.findings);
                process.exit(1);
              }
              console.log('✅ No security issues found');
            "
          else
            echo "ℹ️ dist/security/index.js not found, skipping security scan"
          fi

      - name: Update package version
        run: |
          WORKDIR="tooling/scripts"
          if [ -d "$WORKDIR" ]; then cd "$WORKDIR"; fi
          
          CURRENT_VERSION=$(node -p "require('./package.json').version")
          TARGET_VERSION="${{ steps.version.outputs.version }}"
          
          if [ "$CURRENT_VERSION" = "$TARGET_VERSION" ]; then
            echo "ℹ️ Version already set to $TARGET_VERSION, skipping npm version"
          else
            echo "📦 Updating package.json version from $CURRENT_VERSION to $TARGET_VERSION"
            npm version "$TARGET_VERSION" --no-git-tag-version
          fi

      - name: Setup NPM authentication
        env:
          NPM_TOKEN: ${{ secrets.NPM_TOKEN }}
        run: |
          WORKDIR="tooling/scripts"
          if [ -d "$WORKDIR" ]; then cd "$WORKDIR"; fi

          # Fail early if secret is missing
          if [ -z "$NPM_TOKEN" ]; then
            echo "❌ NPM_TOKEN is not available to this workflow context"
            echo "Ensure the token is defined in repository or org secrets and that this repo has access."
            exit 1
          fi

          # Write ~/.npmrc for auth (preferred for npm CLI)
          mkdir -p "$HOME"
          echo "//registry.npmjs.org/:_authToken=$NPM_TOKEN" > "$HOME/.npmrc"

      - name: Setup NPM authentication (Trusted Publishing)
        if: ${{ inputs.use_trusted_publishing != 'false' }}
        run: |
          WORKDIR="tooling/scripts"
          if [ -d "$WORKDIR" ]; then cd "$WORKDIR"; fi
          # With id-token: write, npm can use OIDC to bypass OTP when publisher trust is configured.
          # No token needed here; provenance will attach build attestation.
          echo "Using npm trusted publishing with GitHub OIDC"
          npm config set registry https://registry.npmjs.org/

      - name: Setup NPM authentication (Token fallback)
        if: ${{ inputs.use_trusted_publishing == 'false' }}
        env:
          NPM_TOKEN: ${{ secrets.NPM_TOKEN }}
        run: |
          WORKDIR="tooling/scripts"
          if [ -d "$WORKDIR" ]; then cd "$WORKDIR"; fi
          if [ -z "$NPM_TOKEN" ]; then
            echo "❌ NPM_TOKEN missing and trusted publishing disabled."
            exit 1
          fi
          mkdir -p "$HOME"
          echo "//registry.npmjs.org/:_authToken=$NPM_TOKEN" > "$HOME/.npmrc"
          echo "always-auth=true" >> "$HOME/.npmrc"
          npm config set registry https://registry.npmjs.org/
          echo "always-auth=true" >> "$HOME/.npmrc"
          npm config set registry https://registry.npmjs.org/

          # Also add project-level .npmrc as fallback
          echo "//registry.npmjs.org/:_authToken=$NPM_TOKEN" > .npmrc
            # For trusted publishing, include provenance to use OIDC and bypass OTP
            if [ "${{ inputs.use_trusted_publishing != 'false' }}" = "true" ]; then
              npm publish --access public --provenance
            else
              npm publish --access public
            fi

          # Verify auth; if whoami fails, print helpful guidance
          if ! npm whoami --registry https://registry.npmjs.org/ >/dev/null 2>&1; then
            echo "⚠️ npm whoami failed (401). Common causes:"
            echo "  - NPM token lacks 'publish' scope or is expired"
            echo "  - Token is a 'Granular Access Token' missing packages:publish scope"
            echo "  - Token is scoped to a different org/user"
            echo "  - The secret isn't exposed to this repository"
            echo "Retrying with explicit config..."
            npm config set //registry.npmjs.org/:_authToken "$NPM_TOKEN"
            npm whoami --registry https://registry.npmjs.org/ || {
              echo "❌ Authentication still failing. Please regenerate an npm 'Access Token' with 'Publish' (automation) scope and update NPM_TOKEN."
              exit 1
            }
          fi

      - name: Publish to NPM
        run: |
          WORKDIR="tooling/scripts"
          if [ -d "$WORKDIR" ]; then cd "$WORKDIR"; fi
          # Publish with public access
          npm publish --access public --registry https://registry.npmjs.org/
        env:
          NPM_TOKEN: ${{ secrets.NPM_TOKEN }}

      - name: Generate SBOM
        run: |
          WORKDIR="tooling/scripts"
          if [ -d "$WORKDIR" ]; then cd "$WORKDIR"; fi
          # Generate Software Bill of Materials if available, else create minimal placeholder
          if [ -f "dist/release/index.js" ]; then
            node --input-type=module -e "
              import { generateSBOM } from './dist/release/index.js';
              await generateSBOM({ format: 'cyclonedx-json', output: 'sbom.json' });
            "
          else
            echo '{"bomFormat":"CycloneDX","specVersion":"1.5","version":1,"metadata":{"component":{"name":"@kitiumai/scripts"}}}' > sbom.json
            echo "ℹ️ dist/release/index.js not found, created placeholder sbom.json"
          fi

      - name: Create GitHub Release
        id: create_release
        uses: actions/create-release@v1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          tag_name: ${{ steps.tag.outputs.tag }}
          release_name: "@kitiumai/scripts ${{ steps.version.outputs.version }}"
          body: |
            ## 🚀 Release @kitiumai/scripts@${{ steps.version.outputs.version }}

            ### What's New
            - Comprehensive collection of reusable scripts and utilities
            - Enhanced security scanning and compliance tools
            - Improved AI provider token management
            - Better dependency management and deprecation handling
            - Extended Git operations and automation capabilities

            ### Installation
            ```bash
            npm install @kitiumai/scripts@${{ steps.version.outputs.version }}
            # or
            pnpm add @kitiumai/scripts@${{ steps.version.outputs.version }}
            ```

            ### Documentation
            📖 [API Reference](https://github.com/kitiumai/scripts/blob/main/API_REFERENCES.md)
            🏠 [Package README](https://github.com/kitiumai/scripts/blob/main/README.md)

            ### Security
            🔒 [SBOM](https://github.com/kitiumai/scripts/releases/download/${{ steps.tag.outputs.tag }}/sbom.json)
          draft: false
          prerelease: false

      - name: Upload SBOM to release
        uses: actions/upload-release-asset@v1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          upload_url: ${{ steps.create_release.outputs.upload_url }}
          asset_path: ${{ github.workspace }}/tooling/scripts/sbom.json
          asset_name: sbom.json
          asset_content_type: application/json

      - name: Notify on success
        if: success()
        run: |
          echo "✅ Successfully released @kitiumai/scripts@${{ steps.version.outputs.version }}"
          echo "📦 Published to NPM: https://www.npmjs.com/package/@kitiumai/scripts/v/${{ steps.version.outputs.version }}"
          echo "🏷️  GitHub Release: https://github.com/kitiumai/scripts/releases/tag/${{ steps.tag.outputs.tag }}"

      - name: Notify on failure
        if: failure()
        run: |
          echo "❌ Failed to release @kitiumai/scripts@${{ steps.version.outputs.version }}"
          echo "Please check the workflow logs for details"