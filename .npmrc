# KitiumAI Package .npmrc Template
# Use this for individual package development
# Copy to: packages/scope/@kitiumai/package-name/.npmrc

# === PUBLIC REGISTRY ===
registry=https://registry.npmjs.org/

# === PRIVATE REGISTRY FOR @kitium-ai SCOPE ===
# Point @kitium-ai scoped packages to GitHub Packages
@kitium-ai:registry=https://npm.pkg.github.com/

# Authentication for private packages
# Set via environment: export NPM_TOKEN=your_github_token
# Or in ~/.npmrc: //npm.pkg.github.com/:_authToken=your_github_token
# //npm.pkg.github.com/:_authToken=${NPM_TOKEN}

# === WORKSPACE ISOLATION ===
# Disable workspace mode for independent package development
workspaces=false

# === DEPENDENCIES ===
# Allow peer dependency mismatches
legacy-peer-deps=true

# Ignore workspace configuration from root
ignore-workspace=true

# === PERFORMANCE ===
# Prefer offline mode (faster installs)
prefer-offline=true

# Use exact versions from package-lock.json
save-exact=false

# === SECURITY ===
# Enable audit for vulnerabilities
audit=true

# === LOGGING ===
# Reduce log verbosity
loglevel=warn

# === EXPERIMENTAL ===
# Faster dependency resolution (npm 7+)
legacy-bundling=false
